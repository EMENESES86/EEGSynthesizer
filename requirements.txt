# ============================================================
#  EEGSynthesizer — requirements.txt
#  Python >= 3.10 recomendado (probado en 3.13.7)
# ============================================================

# --- Core científico ---
numpy>=1.26
pandas>=2.1
scipy>=1.11

# --- EEG / señal biomédica ---
mne>=1.6                  # Lectura EDF del CHB-MIT (mne.io.read_raw_edf)

# --- Machine Learning ---
scikit-learn>=1.4         # RandomForest, LogisticRegression, GroupShuffleSplit, métricas

# --- Visualización ---
matplotlib>=3.8
seaborn>=0.13

# --- Deep Learning (dataloader / modelos futuros) ---
tensorflow>=2.15          # Dataloader con z-score; entrenamiento de modelos downstream

# --- Utilidades de ejecución ---
joblib>=1.3               # Paralelismo implícito en sklearn
tqdm>=4.66                # Barras de progreso (recomendado para pipelines largos)

# --- Jupyter / ejecución de notebooks ---
notebook>=7.0
jupyterlab>=4.0           # Alternativa moderna a notebook clásico
nbconvert>=7.0
ipykernel>=6.0
jinja2>=3.1               # Requerido por nbconvert para templates HTML/LaTeX